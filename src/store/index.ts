import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
    state: {
    },
    // tslint:disable-next-line:object-literal-sort-keys
    mutations: {
    },
    actions: {
    },
    modules: {
    },
});
